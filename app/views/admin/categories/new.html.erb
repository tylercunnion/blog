<h1>New category</h1>

<% form_for [:admin, @category] do |f| %>
 	<%= f.error_messages %>
	<%= render :partial => "form", :object => f %>
	<p>
	  <%= f.submit 'Create' %>
	</p>
<% end %>

<p><%= link_to 'Back', admin_categories_path %></p>