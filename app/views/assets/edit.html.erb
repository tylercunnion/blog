<h1>Editing asset</h1>

<% form_for @asset, :html => {:multipart => true} do |f| %>
  <%= f.error_messages %>
  <%= render :partial => "form", :object => f %>
  <%= f.submit "Update" %>
<% end %>

<%= link_to 'Show', @asset %> |
<%= link_to 'Back', assets_path %>