<h1>File: <%= @asset.file_file_name %></h1>

<% if @asset.file_content_type.split('/')[0] == "image" %>
	<%= image_tag @asset.file.url %>
<% end %>

<p><%= link_to "Link to file", @asset.file.url %></p>

<p><%= link_to 'Edit', edit_asset_path(@asset) %> |
<%= link_to 'Back', assets_path %></p>