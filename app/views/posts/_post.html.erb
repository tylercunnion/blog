<article>
	<header>
		<%= smart_header link_to(post.title, post), header_level %>
	</header>
	
	<%= RDiscount.new(post.text).to_html %>
	
	<footer>
		Posted on <%= time_element(post.created_at) %> by <%=h post.author.name %>
	</footer>
	
</article>
